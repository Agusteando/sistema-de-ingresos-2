name: Control Escolar runner

on:
  push:
    branches: ["chatgpt/control-escolar-resumen"]

permissions:
  contents: write

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - name: Smoke
        run: echo runner-ok
