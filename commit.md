feat(students): derive alta identity fields from CURP

Implement CURP-first personal data capture in the student alta/edit modal.

- Replace manual nacimiento/género controls with a validated CURP field.
- Infer nacimiento, edad, and género from CURP once it is valid.
- Show explicit CURP validation feedback and keep inferred fields hidden until CURP is present.
- Add polished inferred-data animation while preserving the existing modal structure.
- Add a non-mutating matrícula preview for new students; the real matrícula is still generated by the database trigger on save.
- Persist CURP in active student create/update/sync paths and add a defensive base.curp migration.
- Keep alta using the active plantel and selected ciclo de ingreso.
- Preserve proper-name casing before save.

Validation:
- npm ci --ignore-scripts passed.
- npx nuxi prepare passed.
- npm run build completed the client production build; the server transform step exceeded the execution timeout in this environment without surfacing a code error.

Notes:
- The matrícula preview is intentionally informational and does not reserve or create the matrícula.
- Existing dependency audit status remains: 5 vulnerabilities.
