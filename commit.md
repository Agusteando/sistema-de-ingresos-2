Polish expanded account workspace header alignment

- Fix expanded Estado de Cuenta student identity layout so the avatar no longer overlaps the student name or metadata.
- Add explicit compact photo sizing tokens to the shared account photo card for safe reuse in dense layouts.
- Keep the expanded account header structured as a title/status row followed by a separate student identity row.
- Preserve the dense expanded workspace layout and compact default detail view.
