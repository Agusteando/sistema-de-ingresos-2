Restore original KPI visual design while preserving cache-first student sync

- Restore the KPI summary component to the original value rendering and card structure.
- Restore the original KPI stylesheet so the cards match the previous visual design.
- Remove the odometer/delta KPI component and the animated KPI updating state that changed the UI.
- Keep the cache-first student loading flow and subtle sidebar sync status indicator intact.
- Preserve existing student business logic, filters, selections, and background sync behavior.
