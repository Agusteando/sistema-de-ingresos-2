Refine student detail action toolbar

- Keep the student detail primary actions in a single compact row matching the reference layout.
- Replace the detached icon-only overflow control with a labeled Más action inside the same toolbar.
- Move Editar and secondary student actions into the overflow menu to reduce visual noise.
- Remove obsolete toolbar classes so the implementation stays clean and focused on the updated layout.
