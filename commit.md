Improve student filtering and account action density

- Move the active KPI filter pill into the student search control so it no longer consumes a standalone row.
- Compact the student detail action area into primary inline actions with secondary actions available from an overflow menu.
- Add a lightweight Estado de Cuenta search field for filtering concepts by name, month, document, or month key.
- Remove the obsolete active-filter strip component and associated CSS.
